
## Needs
- if base exists, needs to be linked to a pseudo user
- pseudo can only be used by one user


## Soluiton 1 
- when app is first opened, a pseudo is created
- when a user registers, the base is now the main id, pseudo id is updated

logs into a new user
- use the access token to get the pseudo id

